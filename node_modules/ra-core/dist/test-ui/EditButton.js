import * as React from 'react';
import { Link } from 'react-router-dom';
import { useRecordContext } from "../controller/record/useRecordContext.js";
import { useResourceContext } from "../core/useResourceContext.js";
export var EditButton = function (props) {
    var record = useRecordContext(props);
    var resource = useResourceContext(props);
    if (!record)
        return null;
    return (React.createElement(Link, { to: "/".concat(resource, "/").concat(record.id), onClick: function (e) {
            e.stopPropagation();
        } }, "Edit"));
};
//# sourceMappingURL=EditButton.js.map